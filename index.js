'use strict';
/**
 * The actual implementation is on the lib folder.
 * It's a good practice to have an specific index file which only task is to import and
 * export the public parts of the library.
 * It also allows to give more meaningful names to each file.
 */
module.exports = require( './lib/Logger' );